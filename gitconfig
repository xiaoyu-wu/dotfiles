# This is Git's per-user configuration file.
[user]
	name = Xiaoyu Wu
	email = wxy8955@gmail.com
[core]
	editor = vim
[color]
        ui = auto
[alias]
        co = checkout
        cob = checkout -b
        st = status
        amend = commit --amend
        cm = commit
        cmm = commit -m
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
